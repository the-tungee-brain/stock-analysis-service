# Replaced by ranking-pipeline-vm.yml (runs on OCI VM via SSH + docker exec).
# This file is intentionally empty to avoid duplicate runs on GitHub-hosted runners.

name: Ranking pipeline (daily) [deprecated]

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Use workflow: Ranking pipeline (VM)"
          exit 1
